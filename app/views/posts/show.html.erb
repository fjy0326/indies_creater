<h1><%= @post.title %></h1>
<h3><%= link_to @post.user.name, user_path %></h3>
  <table class="table">
    <thead>
      <tr>
        <th>本文</th>
        <th>画像</th>
        <th>音声</th>
        <th colspan="4"></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @post.body %></td>
        
        <% if @post.image.attached? %>
          <td><%= image_tag @post.image, size: "200x200" %></td>
        <% else %>
          <td>No Image...</td>
        <% end %>

        <% if @musics.present? %>
          <td>
            <% @musics.each do |music| %>
              <div class="music">
                <%= @music.body %>
                <audio src="<%= @music.file %>" controls></audio>
              </div>
            <% end %>
          </td>
        <% else %>
          <td>No music available.</td>
        <% end %>
      #<% if current_user == @post.user %>
      <td><%= link_to "Edit", edit_post_path(@post),class: "btn btn-sm btn-success" %></td>
      <td><%= link_to "Destroy", post_path(@post), 
            method: :delete, 
            data: { confirm: "本当に削除しますか？" }, 
            class: "btn btn-sm btn-danger" %></td>
      <% end %>
      </tr>
    </tbody>
  </table>
